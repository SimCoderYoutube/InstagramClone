import { ColorDescriptor } from 'color-string';
/**
 * Creates imageset containing solid color image that is used as a background for Splash Screen.
 */
export default function configureAssets(iosProjectPath: string, color: ColorDescriptor, darkModeColor?: ColorDescriptor): Promise<void>;
